# Server Configuration
# Server bind address (default: 0.0.0.0 - all interfaces)
# SERVER_ADDR=0.0.0.0
PORT=8080

# Proxy Configuration
TARGET_URL=http://localhost:3000

# Service Identity
SERVICE_ID=gokart-sidecar

# Logging Configuration
LOG_LEVEL=info
